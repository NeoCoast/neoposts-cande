.wrapper
  = render partial: 'partials/side-bar', locals: { user: current_user }

  .posts-container.d-flex.flex-column
    - if @posts.present?
      = render partial: 'post', collection: @posts
    - else
      .no-posts
        No posts found
